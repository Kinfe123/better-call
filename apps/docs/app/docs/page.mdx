---
title: "Better Call"
description: "Overview for Better Call."
order: 0
---

# Better Call

Better call is a tiny web framework for creating endpoints that can be invoked as a normal function or mounted to a router to be served by any web standard compatible server (like Bun, node, nextjs, sveltekit...) and also includes a typed RPC client for typesafe client-side invocation of these endpoints.
## What It Helps You Build
The building blocks for better-call are endpoints. You can create an endpoint by calling createEndpoint and passing it a path, options and a handler that will be invoked when the endpoint is called.
## Minimal Example
```ts
import { createEndpoint, createRouter } from "better-call"
import { z } from "zod"

const createItem = createEndpoint("/item", {
    method: "POST",
    body: z.object({
        id: z.string()
    })
}, async (ctx) => {
    return {
        item: {
            id: ctx.body.id
        }
    }
})

// Now you can call the endpoint just as a normal function.
const item = await createItem({
    body: {
        id: "123"
    }
})
```
## Start Here
- [Quickstart](/docs/quickstart) - Run Better Call for the first time.
- [Installation](/docs/installation) - Install and configure Better Call.
## Documentation Map
- Get Started: [Better Call](/docs) - Overview for Better Call.
- Get Started: [Quickstart](/docs/quickstart) - Run Better Call for the first time.
- Get Started: [Installation](/docs/installation) - Install and configure Better Call.
- Core Concepts: [Concepts](/docs/concepts) - Core ideas and mental models that explain how the project works.
- Core Concepts: [Endpoint Creator](/docs/concepts/endpoint-creator) - You can create an endpoint creator by calling createEndpoint.create that will let you apply set of middlewares to all the endpoints...
- Core Concepts: [Endpoints](/docs/concepts/endpoints) - Endpoints are building blocks of better-call.
- Core Concepts: [Error Handling](/docs/concepts/error-handling) - To return a non 200 response, you will need to throw Better Call's APIError error.
- Core Concepts: [Headers and Cookies](/docs/concepts/headers-and-cookies) - If you return a response object from an endpoint, the headers and cookies will be set on the response object.
- Core Concepts: [Middleware](/docs/concepts/middleware) - Endpoints can use middleware by passing the use option to the endpoint.
- Core Concepts: [OpenAPI](/docs/concepts/openapi) - Better Call by default generate open api schema for the endpoints and exposes it on /api/reference path using scalar.
- Core Concepts: [Router](/docs/concepts/router) - You can create a router by calling createRouter and passing it an array of endpoints.
- Core Concepts: [RPC Client](/docs/concepts/rpc-client) - better-call comes with a rpc client that can be used to call endpoints from the client.
- API Reference: [API Reference](/docs/api) - Concrete developer-facing surfaces: commands, exports, adapters, routes, and type contracts.
- API Reference: [Public Exports](/docs/api/exports) - Reference for choosing the correct public import path for the workflow you are building.
- Adapters: [Adapters](/docs/adapters) - Database and persistence adapters for connecting the project to storage layers.
- Adapters: [Node Adapter](/docs/adapters/node) - You can use the node adapter to serve the router with node http server.
## Core Concepts
- [Endpoint Creator](/docs/concepts/endpoint-creator) - You can create an endpoint creator by calling createEndpoint.create that will let you apply set of middlewares to all the endpoints...
- [Endpoints](/docs/concepts/endpoints) - Endpoints are building blocks of better-call.
- [Error Handling](/docs/concepts/error-handling) - To return a non 200 response, you will need to throw Better Call's APIError error.
- [Headers and Cookies](/docs/concepts/headers-and-cookies) - If you return a response object from an endpoint, the headers and cookies will be set on the response object.
- [Middleware](/docs/concepts/middleware) - Endpoints can use middleware by passing the use option to the endpoint.
- [OpenAPI](/docs/concepts/openapi) - Better Call by default generate open api schema for the endpoints and exposes it on /api/reference path using scalar.
- [Router](/docs/concepts/router) - You can create a router by calling createRouter and passing it an array of endpoints.
- [RPC Client](/docs/concepts/rpc-client) - better-call comes with a rpc client that can be used to call endpoints from the client.
## API Reference
- [Public Exports](/docs/api/exports) - Reference for choosing the correct public import path for the workflow you are building.
## Package Details
- Package name: `better-call`
- Runtime and adapter coverage: Express
## Useful Commands
- `test`: `vitest` runs verification for expected behavior.
- `typecheck`: `tsc --noEmit` runs static checks before build or release work continues.
- `build`: `tsup --dts --clean` creates the production build or compiled package artifacts.
- `dev`: `tsup --watch` starts the local development workflow.
